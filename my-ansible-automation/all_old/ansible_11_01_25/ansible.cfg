[defaults]
remote_user = aadarsh
inventory = /home/aadarsh/ansible/inv
ask_pass = false 
gathering = explicit

[privilege_escalation]
become = true
become_user = root
become_ask_pass = false
become_method = sudo

